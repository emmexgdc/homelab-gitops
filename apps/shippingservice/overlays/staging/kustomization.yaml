apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shippingservice-staging

resources:
- ../../base
- configmap.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 0
  target:
    kind: Deployment
    name: shippingservice

images:
- name: shippingservice
  newName: ghcr.io/emmexgdc/shippingservice
  newTag: latest

labels:
- pairs:
    environment: staging
  includeSelectors: false