apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-iuoueywq
  labels:
    app: test-iuoueywq
spec:
  selector:
    matchLabels:
      app: test-iuoueywq
  template:
    metadata:
      labels:
        app: test-iuoueywq
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: test-iuoueywq
        image: test-iuoueywq
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - configMapRef:
            name: test-iuoueywq-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 20
