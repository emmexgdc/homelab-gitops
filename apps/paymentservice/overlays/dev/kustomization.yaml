apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paymentservice-dev

resources:
- ../../base
- configmap.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 1
  target:
    kind: Deployment
    name: paymentservice

images:
- name: paymentservice
  newName: ghcr.io/emmexgdc/paymentservice
  newTag: latest

labels:
- pairs:
    environment: dev
  includeSelectors: false