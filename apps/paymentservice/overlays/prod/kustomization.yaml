apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paymentservice-prod

resources:
- ../../base
- configmap.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 0
  target:
    kind: Deployment
    name: paymentservice

images:
- name: paymentservice
  newName: ghcr.io/emmexgdc/paymentservice
  newTag: latest

labels:
- pairs:
    environment: prod
  includeSelectors: false