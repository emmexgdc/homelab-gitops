apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-react
  labels:
    app: test-react
spec:
  selector:
    matchLabels:
      app: test-react
  template:
    metadata:
      labels:
        app: test-react
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: test-react
        image: test-react
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - configMapRef:
            name: test-react-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 20
