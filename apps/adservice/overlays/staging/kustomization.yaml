apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: adservice-staging

resources:
- ../../base
- configmap.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 2
  target:
    kind: Deployment
    name: adservice

images:
- name: adservice
  newName: ghcr.io/emmexgdc/adservice
  newTag: latest

labels:
- pairs:
    environment: staging
  includeSelectors: false