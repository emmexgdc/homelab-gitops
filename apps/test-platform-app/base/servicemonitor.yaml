apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: test-platform-app
  labels:
    app: test-platform-app
spec:
  selector:
    matchLabels:
      app: test-platform-app
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
