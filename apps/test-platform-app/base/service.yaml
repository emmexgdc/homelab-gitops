apiVersion: v1
kind: Service
metadata:
  name: test-platform-app
  labels:
    app: test-platform-app
spec:
  selector:
    app: test-platform-app
  ports:
  - name: http
    port: 8080
    targetPort: 8080
