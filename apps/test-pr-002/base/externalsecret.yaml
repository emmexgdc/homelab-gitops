apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: test-pr-002
  labels:
    app: test-pr-002
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: test-pr-002-secret
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: apps/test-pr-002
