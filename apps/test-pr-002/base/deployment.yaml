apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-pr-002
  labels:
    app: test-pr-002
spec:
  selector:
    matchLabels:
      app: test-pr-002
  template:
    metadata:
      labels:
        app: test-pr-002
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: test-pr-002
        image: test-pr-002
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - configMapRef:
            name: test-pr-002-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 20
