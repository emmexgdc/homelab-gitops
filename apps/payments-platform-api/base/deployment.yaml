apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-platform-api
  labels:
    app: payments-platform-api
spec:
  selector:
    matchLabels:
      app: payments-platform-api
  template:
    metadata:
      labels:
        app: payments-platform-api
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: payments-platform-api
        image: payments-platform-api
        ports:
        - name: http
          containerPort: 8080
        envFrom:
        - configMapRef:
            name: payments-platform-api-config
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 20
